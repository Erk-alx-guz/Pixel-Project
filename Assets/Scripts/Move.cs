using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerController : MonoBehaviour
{
    //private float speed = 40.0f;
    //private float horizontalInput;
    //private float forwardInput;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        //// This is for player input
        //horizontalInput = Input.GetAxis("Horizontal");
        //forwardInput = Input.GetAxis("Vertical");

        //// We move the pixel forward and back
        //transform.Translate(Vector3.right * Time.deltaTime * speed * forwardInput);
        //// We move the pixel side to side
        //transform.Translate(Vector3.back * Time.deltaTime * speed * horizontalInput);
    }
}
